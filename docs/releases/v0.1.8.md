# Lost Fragments v0.1.8

对比基准：`v0.1.7` -> `v0.1.8`

本版重点完成正式音频接入，移除旧的程序化/占位音效，并补齐首次入梦 Story Book、后期章节视觉、工具图标、发布署名和 release 校验流程相关内容。

## 更新内容

### 音频

- 接入正式 BGM / SFX 资源，移除旧的程序化音效和占位音频。
- 默认音频不再静音，启动后可直接听到音乐和音效。
- 完成标题、场景页、固定剧情、快乐结局、低分/失败结算等 BGM 路由。
- 完成按钮、翻页、走路循环、设置页拉链、战斗抽取/放置/丢弃/碰撞/得分/错误反馈、商店登场、捕梦网等音效路由。
- 音频系统新增 BGM 淡入淡出、SFX 优先级/冷却/并发策略、循环音效管理、音频流缓存和退出清理。

### 剧情与流程

- 新增独立 Story Book 入口场景，用于首次入梦前的剧情翻书流程。
- 主菜单新开局会在无继续存档时进入 Story Book。
- 优化剧情管理器的固定事件和阶段开场队列，减少剧情触发顺序异常。
- Story Book 页面播放期间会锁定无关导航，避免剧情页被误切走。

### 场景、美术与战斗表现

- 接入后期章节的枢纽场景视觉、商人动画，以及父母、小佳、拾忆相关表现资源。
- 接入新的玩家 idle / walk 帧、第五个毛绒过渡帧、5/6 章捕梦网图、拾忆场景背景等资源。
- 修正章节视觉映射，使场景表现按预期剧情顺序推进。
- Boss 节点现在可以按配置加入格子封锁、抽取压力、污染压力等战斗修饰。
- Release 包保留枢纽调试入口，便于快速跳转章节、页面和路线验证。

### 工具、UI 与包体

- 为当前工具接入正式图标，战斗工具栏显示图标和数量。
- 战斗与枢纽战斗展示中加入不可用书签/锁定格子的状态叠加。
- 优化图鉴详情页纸张/图片摆放。
- Windows release 包加入 `CREDITS.txt`，并配置包元数据和应用图标。

### 稳定性与发布流程

- 修复线程预加载场景在退出时可能残留的问题。
- 修复 Cutscene 场景在严格 smoke 测试中会触发空剧情切换的问题。
- 发布脚本会执行 GUT、Python 工具测试、严格场景 smoke 和 Godot Windows 导出，并生成 manifest。
- 增加 Godot 可执行文件、截图、macOS 归档残留等本地文件的忽略规则。

## 关键提交

- `47b3e06` feat(audio): integrate final audio assets
- `0390a52` feat(story): add standalone story book entry
- `b8ea142` feat(tools): add official tool icons
- `1cc3bcc` feat(hub): wire late act visuals and debug flow
- `d66e734` chore(release): include credits in packages
- `890688b` chore(release): configure Windows package metadata
- `33194b8` fix(scene): collect pending scene preloads on exit
- `366aa87` fix(story): keep cutscene smoke checks inert

## 本地验证

- GUT: PASS
- Python tool tests: PASS
- Strict scene smoke: PASS
- Godot Windows export: PASS

备注：本地 Godot 导出进程结束时仍打印了一条非阻塞的 ObjectDB exit warning；当前 precheck 和导出结果均为 PASS，未阻塞包体生成。
