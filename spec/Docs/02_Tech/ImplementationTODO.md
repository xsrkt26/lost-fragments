# 系统实现 TODO

本文档记录当前代码与最新策划案之间的系统级差距。前 13 项为主流程、局内规则、长期构筑与体验表现的基础版本；后续细化项用于记录基础版本之后继续迭代的需求，道具系统从后续细化阶段重新纳入。

后续 Agent 接手开发时，先阅读 `spec/Docs/02_Tech/04_Agent_Development_Workflow.md`，再按本文档优先级推进。`spec/Docs/01_Design/temp` 不作为当前需求来源，除非用户明确要求。2026-05-14 的策划提交变更整理见 `spec/Docs/01_Design/PlanningChangeRequirements_2026-05-14.md`。

## 优先级说明

- P0：主流程骨架，其他系统依赖它接入。
- P1：核心局内规则，会直接影响战斗胜负与长期构筑。
- P2：成长、奖励与经济系统，影响完整一局游戏体验。
- P3：表现、调试与体验完善，不阻塞规则闭环。

## 2026-05-14 策划变更需求队列

来源：`spec/Docs/01_Design/PlanningChangeRequirements_2026-05-14.md`。

1. P0 清理旧术语、旧数据和旧物品池：已完成。移除正式数据中的 `隔离箱`，移除污染层数自动扣梦值的反噬逻辑，用户可见文案统一为梦值/撞击结算口径。
2. P0 重构撞击结算上下文：已完成基础版。`ImpactResolver` 会为每个撞击源创建独立 `ImpactResolutionContext`，记录本次命中物品数、本次命中机械物品数和本次转向传动次数；同一次撞击结算中同一物品最多被命中一次。
3. P1 梦境之种重构：已完成。已从旧版 1-5 级/5x5 形态对齐为 1-30+ 等级与 1x1-4x4 体型，并接入变大失败掉出背包与可监听事件。
4. P1 机械传动正式实现：已完成。已实现左/右传动、双向传动、全向传动和正式 15 个机械物品效果。
5. P1 非道具饰品 v1.2 修正：已完成。命中计数器、终端压力表、齿轮油、万向轴承、反冲片、种子保险、温室玻璃、返青符等已按新版口径核对并接入。
6. P2 道具联动饰品恢复：已完成。51-56 号道具联动饰品已启用，并通过 `after_tool_used`、丢弃监听和撞击上下文接入正式道具系统。
7. F1/P1 道具系统基础版本：已完成。正式 15 个道具池已实现堆叠、使用目标、成功消耗、非法目标不消耗、存档、奖励/商店/事件获得入口和饰品联动。

## P0：先完成主流程骨架

### 1. 局外路线与节点推进

当前状态：已改为路线按钮驱动。Hub 只允许进入当前路线节点，战斗、商店、事件结束后统一推进节点。

实现状态：已完成基础版本。

本次实现范围：

- 新增配置化路线定义，默认路线暂为：局内游戏 -> 商店 -> 事件 -> 局内游戏 -> 商店 -> 事件 -> Boss局内游戏 -> 商店 -> 事件。
- 路线配置集中在 `RouteConfig`，后续可以扩展 `cutscene`、`reward`、`elite_battle` 等节点类型。
- `RunManager` 保存当前路线 ID、当前场景层、当前节点下标和已完成节点。
- Hub 改为路线按钮驱动，只允许进入当前节点，已完成和未解锁节点不可进入。
- 进入节点前会驱动角色移动到对应节点位置。
- 战斗胜利、商店离开、事件确认后会推进到下一个路线节点。
- 事件节点已接入占位事件场景，用于验证路线闭环。
- Boss 战节点已作为节点类型保留，当前暂时进入普通战斗场景。

遗留到后续优先级：

- Boss 分数目标和失败规则在 P0-2 / P0-3 中实现。
- 6 个场景结束后的整局胜利在 P0-3 中实现。
- 路线配置后续可从脚本常量迁移到 `.tres`、JSON 或策划表。

策划目标：

- 玩家开始新游戏后进入局外路线界面。
- 节点按固定顺序推进：局内游戏 -> 商店 -> 事件 -> 局内游戏 -> 商店 -> 事件 -> Boss局内游戏 -> 商店 -> 事件。
- 后续节点可以展示，但当前未解锁节点不可点击。
- 玩家点击当前节点后，角色移动到节点并进入对应场景。
- 战斗、商店、事件结束后回到路线界面，并推进到下一个节点。

实现建议：

- 在 `RunManager` 中新增当前场景、当前节点索引、节点类型、Boss 标记。
- 将 Hub 从自由入口改为路线节点控制器。
- 节点完成后统一通过 `RunManager.advance_node()` 推进。

### 2. 战斗类型与分数目标规则

当前状态：普通战斗默认无分数目标，Boss 战从路线节点读取目标分数；达到目标不会自动结束，战斗结束时统一判定。

实现状态：已完成基础版本。

本次实现范围：

- `RunManager` 新增当前战斗配置接口，统一返回节点类型、是否 Boss、是否有分数目标和目标分数。
- 普通 `battle` 节点默认无分数目标，UI 显示为“无”。
- `boss_battle` 节点必须有分数目标，当前目标公式暂定为 `30 + current_act * 20`。
- 得分达到目标不再自动结束战斗，只改变目标达成时的分数颜色。
- 玩家手动结束或梦值归零时，统一按当前战斗配置结算胜负。
- 无目标普通战斗结束即视为通过并推进路线。
- Boss 未达到目标时整局失败，达到目标时按战斗胜利推进路线。

实现假设：

- 策划案只明确“普通局内游戏不一定有分数目标”，当前默认普通战斗全部无目标；后续如果出现带目标普通战斗，可从路线节点配置中覆盖。
- Boss 目标分数暂用代码公式生成，后续可迁移到路线配置或策划表。
- “梦值归零后等待本次完整结算结束再结束战斗”的严格状态机放到 P1-4 战斗生命周期中实现，本次只统一结束判定入口。

策划目标：

- 普通局内游戏不一定有分数目标，没有目标时显示“无”。
- Boss 局内游戏必须有分数目标。
- 分数目标应在战斗结束时检查，而不是达到目标后立即结束。
- 未完成 Boss 分数要求时整局失败。

实现建议：

- 新增 `BattleConfig` 或在 `RunManager` 中提供当前战斗配置。
- UI 支持目标分数为“无”。
- 移除“达到目标自动胜利”的局内逻辑，改为结束时统一判定。

### 3. 六场景与 Boss 通关结构

当前状态：`RunManager` 已维护当前场景层、路线节点和整局完成状态；第 6 场景路线完成后整局胜利。

实现状态：已完成基础版本。

本次实现范围：

- `RunManager` 新增 `is_run_complete`，用于区分“失败结束”和“整局胜利完成”。
- 路线推进到第 6 场景最后一个节点后，自动触发整局胜利，关闭当前 run，并发出 `run_finished(true)`。
- 整局胜利后继续/返回路线会回到主菜单，不再允许继续进入路线节点。
- 继续游戏按钮改为显示当前场景编号，并在整局完成后禁用。
- 旧存档反序列化默认 `is_run_complete = false`，保持兼容。

实现假设：

- 当前沿用 P0-1 已确认的默认路线：`局内游戏 -> 商店 -> 事件 -> 局内游戏 -> 商店 -> 事件 -> Boss局内游戏 -> 商店 -> 事件`。
- Boss 是当前场景的关键检查点；Boss 后的商店/事件仍视为同一场景的收尾节点。
- 因此“进入下一场景 / 完成第 6 场景”发生在当前场景路线全部节点完成后，而不是 Boss 按钮确认后立即发生。
- 后续如果策划确认 Boss 后应立即切换场景，只需要调整路线配置或在 `advance_route_node()` 对 Boss 节点做特殊完成规则。

策划目标：

- 游戏由 6 个场景构成。
- 每个场景包含固定节点序列，并以 Boss 局内游戏作为关键检查点。
- 通过 Boss 后进入下一个场景。
- 完成第 6 个场景后整局胜利，可以开始新游戏。

实现建议：

- 将 `current_depth` 拆分或替换为 `current_act`、`current_node_index`、`current_battle_index`。
- Boss 胜利后判断是否进入下一场景或结束整局。

## P1：补齐局内核心规则

### 4. 战斗生命周期

当前状态：战斗生命周期已统一到 `BattleManager` 状态机；手动结束和梦值归零都会等待当前结算段落完成后再结算。

实现状态：已完成基础版本。

本次实现范围：

- `BattleManager` 新增轻量战斗状态机：`INTERACTIVE`、`DRAWING`、`RESOLVING`、`FINISHING`、`FINISHED`。
- 新增统一结束请求入口 `request_finish_battle(reason)` 和信号 `battle_finish_requested(reason)`。
- 手动结束和梦值归零都先进入 `BattleManager.request_finish_battle()`，再由 UI 统一结算胜负和结果弹窗。
- 如果结束请求发生在抽取或结算中，会记录为 pending，等当前结算段落结束后再发出结束信号。
- 得分达标不会自动结束，仍需玩家手动结束或梦值归零触发结束请求。
- 战斗最终弹窗出现后调用 `mark_battle_finished()`，阻止重复结束请求。

实现假设：

- 当前“结算段落”覆盖 `request_draw()` 内部抽取、抽到物品效果、全局抽取监听，以及 `_run_impact_sequence()` 撞击序列。
- 更细的输入锁、动画回调和多段异步结算会在 P3-12 捕梦表现与抽取区域中继续细化。

策划目标：

- 捕梦后先完整结算抽到物品和背包内物品效果。
- 梦值归零后，在本次结算完成后自动结束战斗。
- 玩家也可以手动结束战斗。
- 战斗结束时统一处理胜负、奖励和战后清理。

实现建议：

- 给 `BattleManager` 增加明确的战斗状态机：抽取中、结算中、可操作、结束中。
- 将 `GameState.game_over` 从“立即失败”改为“请求结束战斗”。
- 战斗结束统一走一个 `finish_battle()` 入口。

### 5. 背包跨战斗状态

当前状态：背包布局已跨战斗保存；背包外物品战后丢弃，污染和衍生物品不跨战斗保存。

实现状态：已完成基础版本。

本次实现范围：

- `RunManager` 新增 `current_backpack_items`，保存跨战斗背包布局。
- 战斗结束弹窗前，先丢弃背包外物品，再保存背包内物品状态。
- 保存字段包含：物品 id、根坐标、方向、形状和 runtime id。
- 污染不保存，下一场战斗恢复时从 0 层开始。
- 带有 `衍生物品` 标签的物品不保存，下一场战斗不会恢复。
- 新战斗初始化时，`BattleManager` 从 `RunManager.current_backpack_items` 重建背包逻辑实例。
- `MainGameUI` 会为已恢复的背包物品生成对应 `ItemUI`，保持可拖拽、可旋转、可丢弃。
- 存档序列化新增 `backpack_items` 字段，旧存档默认空背包，保持兼容。
- 物品变身、进化或替换数据时会保留原 `runtime_id`，并通知战斗 UI 同步物品显示与映射，避免苹果核变回苹果等效果后丢失拖拽、悬浮或动画定位。
- 新 run 默认在背包第三排左侧加入 `root_dream`，作为新版设计中替代固定撞击源的根源之梦；旧存档恢复时若缺失该物品，会自动补入一个根源之梦。

自动化测试：

- `test/unit/test_backpack_persistence.gd` 覆盖背包状态保存、衍生物品过滤、污染不跨战斗保存，以及恢复时保留位置、方向、形状和 runtime id。
- `test/unit/test_backpack_persistence.gd` 额外覆盖新 run 默认根源之梦、旧存档恢复补根源之梦。
- `test/unit/test_backpack_manager.gd` 覆盖替换同形状和不同形状物品时保留 `runtime_id`，并发出替换事件。

实现假设：

- 当前只持久化背包内物品；背包外未放置物品仍按策划在战斗结束时自动丢弃。
- “保留状态”字段尚未有明确数据模型，本次未新增独立保留标记；后续如果策划需要锁定/保留类效果，可在 `current_backpack_items` 中追加字段。
- 当前通过 `衍生物品` 标签判断战后移除，例如 `apple_core`。

策划目标：

- 进入局内时展示玩家当前背包物品布局。
- 背包内物品的摆放、旋转和保留状态需要在战斗之间延续。
- 背包外物品在战斗结束时自动丢弃。
- 污染在每场战斗结束后清除。
- 衍生物品在每场战斗结束后从背包中消失。

实现建议：

- 在 `RunManager` 中保存背包物品实例列表，包括物品 id、位置、方向、形状、运行时状态。
- 战斗开始时从运行数据重建背包。
- 战斗结束时写回背包状态，并执行污染清理与衍生物品移除。

### 6. 播种与梦境之种成长

当前状态：播种、梦境之种真实等级、体型阈值、变大失败掉出背包和相关事件总线信号已接入 `BackpackManager`。

实现状态：已完成基础版本。

本次实现范围：

- `BackpackManager` 新增统一入口 `sow_seed(source_instance, direction, item_db, levels = 1)`。
- 如果来源物品指定方向第一格为空，生成 `dream_seed_1x1`。
- 通过播种生成的梦境之种会追加 `衍生物品` 标签，战斗结束后不会跨战斗保存。
- 如果指定方向第一格已有梦境之种，则调用 `upgrade_seed()` 进行等级提升。
- 梦境之种以运行时 `dream_seed_level` 记录真实等级：1-9 为 1x1，10-19 为 2x2，20-29 为 3x3，30 及以上为 4x4。
- 4x4 后继续升级只提高等级，不再继续变大；正式资源仅保留 `dream_seed_1x1` 到 `dream_seed_4x4`。
- 升级变大时保留 runtime id、方向和衍生标签；如果升级后形状放不下，会从背包中移出并发出 `seed_sow_failed` 事件，供 `种子保险` 等饰品监听。
- 梦境之种被撞时按当前等级和体型阶段给分：1-9 级 x1，10-19 级 x2，20-29 级 x4，30 级及以上 x8。
- `GlobalEventBus` 新增 `seed_sown(instance)` 与 `seed_upgraded(instance, old_level, new_level)`，供后续饰品、道具和奖励监听。

实现假设：

- “所指方向第一格”按来源物品形状边缘外的第一格计算；多格物品同方向有多个候选格时使用左上优先。
- 当前只是底层统一入口；具体梦境流物品和道具效果接入时复用 `sow_seed()` / `upgrade_seed()`。

策划目标：

- 播种时，如果指定方向第一个格子为空，生成 1 级梦境之种。
- 如果目标格已有梦境之种，则梦境之种等级 +1；达到 10/20/30 级时尝试变为 2x2/3x3/4x4。
- 梦境之种升级需要触发相关物品和饰品效果。
- 梦境之种属于衍生物品时，应遵循战后清理规则。

实现建议：

- 新增统一的 `sow_seed()` / `upgrade_seed()` 逻辑入口。
- 通过事件总线广播播种、升级、收获等事件，供物品和饰品监听。

自动化测试：

- `test/unit/test_seed_growth.gd` 覆盖播种生成、重复播种升级、10 级变为 2x2、空间不足掉出背包、30 级后保持 4x4 继续升级，以及被撞收益按当前等级和阶段倍率结算。
- `test/unit/test_ornament_system.gd` 覆盖 `种子保险` 对变大失败掉出背包的补偿，以及 `温室玻璃` / `返青符` 对 10/20/30 阈值的新版语义。

### 7. 多源撞击调度

当前状态：多源撞击已统一进入 `BattleManager` 撞击队列，并按左上优先级和入队序号确定性结算。

实现状态：已完成基础版本。

本次实现范围：

- `BattleManager` 新增统一撞击队列 `queue_impact_at(pos, direction, source, reason)`，旧入口 `trigger_impact_at(pos)` 保留并改为入队。
- 队列项记录来源物品、方向、触发原因、左上优先级和入队序号；优先级按 `y * grid_width + x` 计算，保证同一行从左到右、行与行从上到下。
- `request_draw()` 在抽取物品和全局抽取监听结束后，会等待本结算窗口内的撞击队列处理完成，再回到可操作状态或发出 pending 结束请求。
- 每次只弹出一次撞击结算，等待 `ImpactResolver` 与 `SequencePlayer` 完整结算后再处理下一次；结算过程中新增的撞击继续进入同一队列并重新排序。
- `interval_trigger`、`reactive_impact`、`same_item_draw`、`stain_magnifier`、`tag_reactive` 等延迟撞击效果已统一改为入队，不再直接 `call_deferred("trigger_impact_at")`。
- `root_dream` 挂载 `interval_trigger`，配置为每捕梦 5 次入队发起一次撞击，不需要污染层数。

实现假设：

- “同时发起”以同一抽取/撞击结算窗口内进入队列的撞击为准；新入队的撞击会参与剩余队列排序，但不会打断已经开始播放的当前撞击结算。
- 如果来源物品已离开背包或不再有效，该队列项会被跳过，避免已删除物品触发幽灵撞击。

自动化测试：

- `test/unit/test_impact_queue.gd` 覆盖左上优先级排序、同窗口新增撞击重新排序、空格/失效来源拒绝，以及旧 `trigger_impact_at()` 入口兼容入队。
- `test/unit/test_impact_queue.gd` 覆盖根源之梦在第 5 次捕梦后进入撞击队列。

策划目标：

- 多个物品同时发起撞击时，按左上优先级排序。
- 每次撞击结算完整结束后，再处理下一次撞击结算。
- 同一结算窗口内的新增撞击需要进入队列，避免顺序不确定。

实现建议：

- 新增撞击队列，由 `BattleManager` 或专门的 `ImpactQueue` 管理。
- 队列项记录来源物品、方向、优先级和触发原因。
- 所有延迟撞击统一入队，不直接 `call_deferred("trigger_impact_at")`。

### 7.1 机械传动正式实现

当前状态：正式机械物品池和机械专用传动关键词已接入 `ImpactResolver` 与物品数据。

实现状态：已完成基础版本。

本次实现范围：

- `ItemData.TransmissionMode` 扩展 `MECHANICAL_LEFT`、`MECHANICAL_RIGHT`、`MECHANICAL_BIDIRECTIONAL`、`MECHANICAL_OMNI`，保持旧枚举值兼容。
- `ImpactResolver` 支持机械撞击分支：分支只命中紧贴的 `机械` 物品，遇到空格或非机械物品时停止。
- 向左/向右传动按当前物品朝向计算相对方向；双向传动在本次命中机械物品数达到 3 时向左右两侧发起机械撞击；全向传动按上、右、下、左顺序尝试相邻机械物品。
- 同一次撞击结算继续复用 `ImpactResolutionContext` 去重，并新增 `bidirectional_transmission_count`、`successful_mechanical_transmission_count` 统计。
- `本次撞击结算全部停止后` 的物品效果通过 `after_resolution()` 在整个 `ImpactResolver` 结束后统一触发。
- 新增 `MechanicalItemEffect`，集中实现正式机械物品的分数段，传动段由 `ImpactResolver` 负责，避免污染倍率重复复制传动分支。
- 新增正式 15 个机械物品资源：`铁珠`、`小齿轮`、`传动皮带`、`左传动弯管`、`右传动弯管`、`制动片`、`齿条`、`差速器`、`双轴转轮`、`蓄能飞轮`、`曲轴`、`计数轮`、`中央引擎`、`终端计算机`、`星环轴承`。

实现假设：

- 左/右/双向/全向传动模式本身只生成对应机械分支，不额外执行普通同向传导；需要普通同向传导的机械物品继续使用 `NORMAL`。
- `星环轴承` 的“每个局内关卡第一次”以运行时物品实例 meta 记录，战斗重建后自然重置；由星环轴承生成的全向分支会带上禁止再次触发星环轴承的分支标记。
- `蓄能飞轮` 按本次结算最终命中机械物品数统一结算，最多给 2 次 +12 分。

自动化测试：

- `test/unit/test_impact_propagation.gd` 覆盖左传动相对方向、机械分支被非机械紧贴物阻断、双向传动统计、结算停止后物品得分、曲轴右传动命中奖励、星环轴承每关一次与嵌套抑制。
- `test/integration/test_latest_card_design.gd` 覆盖正式物品池新增 15 个机械物品并继续排除 `隔离箱`。

## P2：补齐长期构筑与经济

### 8. 饰品系统

当前状态：饰品总表已对齐 56 个。全部饰品已有数据、运行时状态、局内栏位、商店/奖励/事件获得入口和基础效果；51-56 号道具联动饰品已随 F1 道具系统恢复启用。

实现状态：已完成 56 个饰品的数据框架、50 个非道具饰品基础效果，以及 6 个道具联动饰品的道具触发效果。

本次实现范围：

- 新增 `OrnamentData`、`OrnamentEffect` 与 `OrnamentDatabase`，并作为 autoload 加载。
- 新增 `data/ornaments/ornaments.json`，录入饰品基础数据：id、名称、分类、稀有度、最早出现层、价格、标签、效果文本、效果 id 与运行时启用状态。
- 51-56 号道具联动饰品已启用，并绑定 `tool_belt`、`specimen_pin_case`、`gardening_toolkit`、`recycling_hook`、`calibration_screwdriver`、`universal_toolbox` 等实际效果 id。
- `OrnamentDatabase.get_all_ornaments()` 返回完整 56 个总表项，`get_available_ornaments()` 只返回已启用饰品，奖励和商店统一复用该入口。
- `RunManager.current_ornaments` 保持获得顺序，并新增 `add_ornament()`、`remove_ornament()`、`has_ornament()`，同名饰品不能重复获得。
- `BattleManager` 在战斗初始化时按获得顺序加载饰品运行时状态，捕梦梦值损失先走饰品减免，再进行扣除；捕梦后与本次撞击结算结束后按获得顺序触发饰品效果。
- 局内饰品栏会渲染已获得饰品，并通过悬停 tooltip 显示名称、稀有度、最早出现层、标签和效果。
- 已实现前 5 个通用饰品效果：`旧怀表`、`捕梦滤网`、`回音耳坠`、`导向罗盘`、`安全别针`。
- 新增通用饰品运行时效果 `GenericOrnamentEffect`，将其余非独立脚本饰品全部绑定到非空效果 id；道具联动饰品通过 `after_tool_used`、丢弃监听和撞击上下文触发。
- 扩展 `OrnamentEffect` 触发点：局内开始、捕梦、放置、丢弃、本次撞击结算结束、播种成功、播种失败、种子升级、污染增加和净化。
- `BattleManager` 统一转发上述触发点，保证多个饰品仍按获得顺序结算。
- `BackpackManager` 与 `GlobalEventBus` 新增污染变化和播种失败事件，用于污染、净化和播种类饰品监听。
- `回收券` 接入商店购买价格：每个商店首次购买物品后，下一个物品价格按 80% 结算。
- `空梦奖杯` 接入关卡奖励入口：有目标分数且超过目标 50 分以上时，下一次奖励选项数量 +1；无目标分数的普通局内游戏不触发。

实现假设：

- 剩余非独立脚本饰品使用统一通用效果类按 `effect_id` 分发，避免为每个简单数值饰品创建重复脚本。
- 道具联动饰品在独立道具栏、堆叠、目标校验和使用事件完成后进入可用池；后续若策划调整数值，只需改饰品表和 `GenericOrnamentEffect` 中对应分支。
- `导向罗盘` 当前在根源之梦空撞结算完成后直接旋转来源物品方向，用作“下一次根源之梦撞击方向顺时针旋转”的状态表达。
- `万向轴承`、`反冲片` 等会改变撞击传导结构的饰品，当前通过战斗管理器的确定性撞击队列追加额外撞击表达，不在 `ImpactResolver` 中途改写本条链的扫描规则。
- `回收券` 的折扣在购买结算时生效，商店 UI 会在成功购买后刷新未购买商品显示价格。
- `空梦奖杯` 当前按奖励选项总数 +1 实现；后续如果需要“只增加饰品选项而不增加总选项”，需要扩展 `RewardGenerator` 的饰品槽位控制。

自动化测试：

- `test/unit/test_ornament_system.gd` 覆盖 56 个饰品总表加载、全部饰品启用、每个饰品绑定非空效果、按层数过滤、重复获得拒绝、前 5 个通用饰品效果，以及通用饰品在捕梦消耗、污染变化、丢弃、播种升级、撞击结算阈值和商店折扣中的核心行为。
- `test/unit/test_reward_system.gd` 与 `test/unit/test_shop_system.gd` 覆盖奖励和商店可产出已启用的道具联动饰品。

策划目标：

- 实现 56 个饰品的数据表和资源加载；道具联动饰品在道具系统完成后进入可获得池。
- 饰品不占背包格、不参与撞击、不能重复获得。
- 饰品通过商店、事件、关卡奖励或 Boss 奖励获得。
- 鼠标悬停显示名称、稀有度、最早出现层、标签、效果和说明。
- 多个饰品同时触发时按获得顺序结算。
- 数值修改类效果先结算“减少消耗”和“改变规则”，再结算“获得分数”和“获得梦值”。

实现建议：

- 新增 `OrnamentData`、`OrnamentDatabase`、`OrnamentEffect`。
- 将饰品触发点接入捕梦、撞击结算、污染、净化、播种、丢弃、战斗结束等事件。
- 先实现通用饰品和少量污染/机械饰品，再扩展完整 50 个。

### 9. 关卡奖励与 Boss 奖励

当前状态：战斗胜利会展示奖励选项，奖励可写入碎片、物品、饰品或道具；Boss 奖励有更高稀有倾向。

实现状态：已完成基础版本。

本次实现范围：

- 新增 `RewardGenerator`，统一生成奖励选项，奖励类型包含物品、饰品、碎片和道具。
- 普通战斗胜利默认生成 3 个选项：可加入卡组的物品、当前层可获得且未拥有的饰品、碎片；需要第 4 个奖励槽时可补充道具。
- Boss 战奖励使用同一入口，但优先选择更高稀有度和更高价格的饰品；第 4 层及以后会优先提供稀有饰品。
- `RunManager` 新增 `generate_current_reward_options()` 与 `apply_reward()`，奖励写入长期状态：碎片增加、物品加入卡组、饰品按获得顺序加入且不能重复、道具进入独立堆叠库存。
- 胜利弹窗改为显示奖励选项，玩家选择奖励后再调用 `win_battle(0)` 推进路线；固定碎片奖励不再直接发放。

实现假设：

- 当前奖励生成是确定性基础版本，用于验证奖励管线；后续可以在 `RewardGenerator` 内替换为带权重随机、按流派倾向、按已有构筑过滤的奖励池。
- 物品奖励当前直接加入卡组；如果后续策划要求“加入背包”或“暂存待摆放”，在 `RunManager.apply_reward()` 的物品分支扩展。
- 选择奖励是必选流程；无可用奖励时才显示普通继续按钮。

自动化测试：

- `test/unit/test_reward_system.gd` 覆盖普通战斗奖励类型、Boss 稀有饰品优先级，以及碎片、物品、饰品、道具奖励写入长期状态和重复饰品拒绝。

策划目标：

- 普通关卡和 Boss 结束后可以提供奖励。
- 奖励可以包含物品、饰品、碎片、道具或其他长期资源。
- Boss 奖励应优先提供更高价值的构筑方向。

实现建议：

- 新增奖励池和奖励选择 UI。
- 奖励生成基于当前场景层数、节点类型、稀有度和已有构筑。

### 10. 商店系统

当前状态：商店按当前节点缓存库存，支持刷新、价格曲线、构筑倾向、饰品过滤、道具商品和物品暂存去向。

实现状态：已完成基础版本。

本次实现范围：

- 新增 `ShopGenerator`，统一生成商店商品，商品类型包含物品、饰品和道具。
- 商店物品池从 `ItemDatabase.get_all_items()` 获取，只出售可抽取物品，并按价格与 id 稳定排序。
- 商店饰品池从 `OrnamentDatabase.get_available_ornaments()` 获取，按当前场景层数和已拥有饰品过滤，同名饰品不会出现在货架中。
- `RunManager` 新增 `generate_current_shop_offers()` 与 `buy_shop_offer()`，购买会统一扣除碎片并写入长期状态：物品加入卡组，饰品加入 `current_ornaments`，道具进入独立堆叠库存。
- 商店 UI 改为展示物品/饰品/道具类型、稀有度和价格，悬停显示描述；购买成功后对应按钮禁用为“已购买”。
- 离开商店仍沿用路线节点推进逻辑，允许玩家买完后主动返回路线。

实现假设：

- 当前商店生成是确定性基础版本，优先验证商品池与购买管线；后续可在 `ShopGenerator` 中替换为带权重随机、库存刷新和按构筑倾向过滤。
- 物品购买当前直接加入卡组；如果后续策划要求购买后进入背包或暂存区，需要扩展 `buy_shop_offer()` 的物品分支。
- 饰品使用策划表推荐价格；前期可能出现买不起的饰品，这是经济数值调试项，当前不在本次逻辑实现范围内。

自动化测试：

- `test/unit/test_shop_system.gd` 覆盖商店物品/饰品/道具商品生成、已拥有饰品过滤、购买扣碎片、物品入卡组、饰品入长期状态、道具入堆叠库存和重复饰品购买拒绝。

策划目标：

- 商店作为局外路线节点出现。
- 商店应支持物品、饰品和道具出售。
- 商品池需要按层数、稀有度、价格和已拥有内容过滤。
- 购买后更新长期构筑状态，并推进或允许返回路线。

实现建议：

- 将商店商品生成从简单随机改为基于奖励池。
- 接入饰品商品、价格、库存和刷新规则。
- 购买物品时需要明确加入卡组或背包的规则。

### 11. 事件系统

当前状态：事件系统已完成基础选择、权重随机、事件去重、节点缓存、事务回滚和高风险背包事件确认。

本次实现范围：

- 新增 `data/events/events.json`，首批事件包含碎片、物品、饰品、梦值回复、背包空间扩展和带碎片成本的风险选择。
- 新增 `EventData` 与 `EventDatabase`，作为 autoload 统一加载事件池，并按当前场景层过滤可用事件。
- `RunManager` 新增 `apply_event_choice()`，事件效果统一写入长期状态：碎片、物品、饰品、背包可用区域和梦值。
- `RunManager` 新增背包可用区域长期字段，默认 7x7 总格、5x5 可用，事件最多扩展到 7x7；`BattleManager` 初始化时读取该配置。
- 事件场景从占位确认改为选择式 UI，玩家选择事件选项后推进路线并回到 Hub；有待选择项时不会通过 ESC 跳过事件。

实现假设：

- 事件池当前使用确定性选择，按当前层数和路线节点位置从可用事件中取一项，用于保证测试和调试稳定；后续可以替换为带权重随机和事件去重。
- 事件获得物品当前沿用奖励系统语义，直接加入卡组；如果后续策划要求进入背包或暂存区，需要扩展事件效果类型。
- 梦值回复通过当前 `GameState` 立即生效，主要用于局外事件进入下一场战斗前的状态调整；若后续需要跨场景梦值上限或长期梦值资源，应拆出独立运行状态字段。
- 背包变化当前只实现“可用区域扩展”，未实现删格、锁格、强制移动已有物品等高风险变化。

自动化测试：

- `test/unit/test_event_system.gd` 覆盖事件池加载与层数过滤、确定性事件选择、事件选项写入长期状态、成本不足/重复饰品时回滚，以及背包空间扩展的存档和战斗初始化读取。

策划目标：

- 事件是局外路线节点之一。
- 事件可以提供物品、饰品、碎片、梦值、背包变化或风险选择。
- 事件结束后返回路线并推进到下一个节点。

实现建议：

- 新增 `EventData`、`EventDatabase` 和事件选择 UI。
- 先实现少量基础事件，用于验证路线推进和奖励系统。

## P3：体验与表现完善

### 12. 捕梦表现与抽取区域

当前状态：捕梦按钮已有输入锁、轻量动画、抽取出生点配置和物品生成动画；正式动画/美术/CG 等待素材替换。
实现状态：已完成基础版本。
本次实现范围：

- 捕梦按钮在动画、抽取和结算期间会进入输入锁定状态，非 `INTERACTIVE` 战斗状态下不可重复点击捕梦。
- 点击捕梦后会先播放捕梦面板缩放动画，再进入 `BattleManager.request_draw()` 抽取流程。
- 抽到的物品会从 `DrawSpawnPoint` 配置节点位置生成，不再写死在脚本坐标中。
- 抽到的物品生成时增加淡入和缩放动画，保留后续拖拽、旋转、放置和丢弃交互。

实现假设：

- 当前动画先使用现有捕梦面板做轻量 tween 表现；后续有正式捕梦动画、美术或 CG 时，可替换 `_play_dreamcatcher_animation()`，不需要改动抽取状态机。
- 抽取出生点属于 UI 布局职责，当前做成 `main_game_ui.tscn` 中的可配置节点；如果后续同一套逻辑需要支持多套布局，可以继续扩展为外部配置表。

自动化测试：

- `test/unit/test_battle_target_rules.gd` 覆盖捕梦输入锁对按钮禁用状态的影响，以及非交互战斗状态下不能触发捕梦交互。

策划目标：

- 点击捕梦按钮后播放捕梦动画。
- 物品出现在策划指定的背包外预设位置。
- 捕梦、结算、拖拽之间的状态切换清晰，不允许结算中重复操作。

实现建议：

- 给捕梦流程加入动画回调和输入锁。
- 将抽到物品的出生点配置化。

### 13. UI 信息完整性

当前状态：局内梦值、分数、目标显示、背包、垃圾桶、饰品栏、道具栏、tooltip、暂存物品面板和奖励选择 UI 已接入基础版本。
实现状态：已完成基础版本。
本次实现范围：

- 分数目标为“无”的规则状态已接入局内显示。
- 饰品栏已展示已获得饰品，并支持悬停查看名称、稀有度、层数、标签和效果说明；道具栏已展示当前堆叠数量、悬停说明、点击选择和目标高亮状态。
- 物品卡牌已接入全局悬浮说明，背包内物品和商店物品商品都能显示物品描述；背包内污染变化会实时刷新物品 UI 标记与当前污染层数。
- 背包格子增加悬停说明：锁定格显示“锁定格”，可用空格显示“可用格”，已占用格显示物品名称。
- 捕梦按钮和丢弃区增加悬停说明，降低只靠视觉图标判断的成本。

自动化测试：

- `test/unit/test_card_tooltip.gd` 覆盖物品污染层数变化后的 UI 同步、全局卡牌悬浮说明的动态污染显示，以及空物品数据不会保留旧悬浮窗。
- `test/unit/test_shop_system.gd` 覆盖商店物品商品使用全局卡牌悬浮说明。
- `test/integration/test_scene_smoke.gd` 作为每次 AI 改动后的固定 headless 场景冒烟测试，覆盖 `src/ui/main_menu/main_menu.tscn`、`src/ui/hub/hub_scene.tscn`、`src/ui/main_game_ui.tscn`、`src/ui/shop/shop_scene.tscn` 和 `src/ui/debug/debug_sandbox.tscn`。

实现假设：

- 本次优先补齐规则和交互状态可读性，最终视觉规范、图标和动画资产仍需在 UI 视觉迭代中统一替换。

体验补丁：

- Hub 中整理背包浮层已增加鼠标可点击的关闭按钮，不再只能依赖 ESC。
- Hub 角色已支持鼠标点击移动，键盘移动仅作为兼容输入保留。
- `main_game_ui.tscn` 新增整理背包浮层模式：Hub 打开整理背包时保持 `UI` 输入上下文，不再延迟切回 `BATTLE`、不重置局内状态、不播放战斗 BGM；关闭按钮和 ESC 都走同一关闭并保存背包布局入口。
- 整理背包浮层会隐藏局内捕梦、战斗结束、梦值/分数和饰品栏等战斗专属面板，并将背包面板居中显示，降低 Hub/局内复用时的覆盖和层级重叠。
- `test/unit/test_hub_player.gd` 覆盖整理背包浮层关闭按钮、关闭后恢复 `WORLD` 输入上下文、浮层保持 `UI` 上下文，以及 Hub 鼠标点击移动。

策划目标：

- 分数区域支持目标为“无”。
- 饰品栏展示已获得饰品并支持悬停说明；道具栏展示已获得道具并支持点击或拖放到合法目标。
- 背包锁定格、可用格、丢弃区和抽取区需要与最终 UI 规范一致。

实现建议：

- 先补齐规则状态展示，再统一做视觉迭代。

## 建议实施顺序

1. 局外路线与节点推进。
2. 战斗类型与分数目标规则。
3. 六场景与 Boss 通关结构。
4. 战斗生命周期统一入口。
5. 背包跨战斗状态。
6. 播种与梦境之种成长。
7. 多源撞击调度。
8. 饰品数据框架和前 5 个通用饰品。
9. 关卡奖励与 Boss 奖励。
10. 商店系统改造。
11. 事件系统。
12. 饰品完整实现。
13. 捕梦表现与 UI 信息完善。（已完成基础版本）

## 后续细化项

以下需求不是当前主流程闭环的阻塞项，但会影响完整版本的可玩性、构筑深度、数值稳定性和发布质量。后续开发继续遵循“梳理需求与当前实现 -> 确认需求 -> 架构设计与实现 -> 自动化测试 -> 文档更新 -> commit/push”的流程。

### F1. 道具系统基础版本（已完成）

优先级：P1

当前状态：基础版本已完成。道具使用独立数据、库存和 UI，不放入普通背包格；正式 15 个道具池已实现堆叠、使用目标校验、成功消耗、非法目标不消耗、存档恢复、奖励/商店/事件写入和 51-56 号饰品联动。

需求范围：

- 道具属于特殊物品，不放入普通背包格参与撞击结算。
- 道具有独立道具栏或堆叠显示，同一种道具可以堆叠。
- 玩家可以点击或拖拽道具到合法目标上使用。
- 使用成功后数量 -1；目标不合法时不消耗并回到原位。
- 道具可通过商店、事件、关卡奖励、饰品效果或物品效果获得。
- 基础版本实现正式 15 个道具，复用现有播种、污染、净化、捕梦消耗、撞击、抽取、奖励和背包状态接口。

完成记录：

- 新增 `data/tools/tools.json`，录入 15 个正式道具：小补丁、梦值糖、转向螺丝、破弹珠、黑墨滴、消毒喷雾、腐蚀酸、小水滴、肥料包、快速发芽剂、延长钩、传动油、苹果蜡、回收夹、空白符纸。
- 新增 `ToolData`、`ToolDatabase`、`ToolEffect`；道具目标类型覆盖 `item`、`empty_cell`、`item_or_empty_cell`、`dreamcatcher`、`discard`、`ornament`。
- `RunManager.current_tools` 保存长期道具堆叠，并通过 `tools_changed` 广播给 UI；存档、事件回滚、奖励、商店和事件效果均支持 `tool` 类型。
- `BattleManager.request_use_tool()` 统一处理局内道具使用：先校验目标和执行效果，成功后消耗 1 个；非法目标、缺少道具或效果失败时不消耗。
- 局内 UI 新增道具栏，支持点击选择、释放到目标、悬停 tooltip、数量显示和使用失败反馈；合法目标包含背包物品、背包空格、捕梦区、垃圾桶和饰品槽。
- 15 个道具效果已接入：本局价值修正、下一次捕梦消耗减免、旋转并得分、从目标发起撞击、污染/净化、梦境之种播种/升级、机械延长/传动加分、食物丢弃回血/播种、下一次丢弃废弃物得分并获得纸团、刷新单局限次饰品触发。
- 51-56 号道具联动饰品已启用，分别覆盖首次/累计使用、污染/净化目标、播种/升级、丢弃废弃物、机械目标校准、三类目标集合奖励等基础效果。

实现假设：

- 策划文档列出了道具基本规则和正式道具池，但未给出完整掉落/商店权重；当前按技术假设把道具接入奖励第 4 槽、商店库存池、事件效果和饰品/物品效果入口，后续可按策划数值表调权重与价格。
- `回收夹` 文案中的“纸团”映射到现有正式物品 `paper_ball`；如果后续策划需要“皱纸团”作为新物品，应新增正式物品数据后再调整。
- 道具联动饰品当前使用低风险数值版本，主要验证触发点和状态管理；最终数值可直接在饰品表和通用效果分支中迭代。

自动化测试：

- `test/unit/test_tool_system.gd` 覆盖道具池加载、堆叠、合法目标消耗、非法目标不消耗、存档恢复、关键效果和饰品联动。
- `test/unit/test_reward_system.gd`、`test/unit/test_shop_system.gd`、`test/unit/test_ornament_system.gd` 覆盖奖励/商店写入道具库存、道具联动饰品启用和可获得。
- 本轮已通过 `tools/run_tests_silent.ps1` 全量 GUT 与 `python -B scripts/run_scene_smoke_tests.py --fail-on-engine-error` 严格场景冒烟。

### F2. 路线配置外置

优先级：P2

当前状态：已完成基础版本。默认路线已迁移到 `data/routes/routes.json`，代码中保留内置默认路线作为配置缺失或非法时的安全回退。

需求范围：

- 将默认路线从脚本常量迁移到 `.tres`、JSON 或策划表。
- 支持新增节点类型，例如 `cutscene`、`reward`、`elite_battle`。
- 允许不同路线或不同场景层覆盖节点顺序、标签、目标配置和展示资源。

自动化测试建议：

- 已覆盖配置加载、非法配置回退、节点类型映射、旧存档兼容。

完成记录：

- `RouteConfig` 作为路线配置唯一读取入口，负责读取 JSON、规范化节点、回退默认路线和解析节点场景键。
- 默认路线保持 9 个节点不变：局内游戏 -> 商店 -> 事件 -> 局内游戏 -> 商店 -> 事件 -> Boss局内游戏 -> 商店 -> 事件。
- 路线配置支持 `id`、`type`、`label`、`scene`、`score_target`、`metadata` 字段。
- 新增节点类型常量 `cutscene`、`reward`、`elite_battle`，其中 `elite_battle` 归入战斗节点，`reward` 和 `cutscene` 可通过 `scene` 字段映射到现有场景占位。
- Boss 分数目标从节点配置读取，当前默认配置保持 `30 + current_act * 20` 的旧规则。
- `RunManager` 保持旧存档兼容；旧存档没有路线字段或路线 ID 非法时仍回到默认路线。
- 全量 GUT 已通过，固定关键场景 headless 冒烟列表同步通过。

### F3. 奖励、商店与事件随机规则

优先级：P2

当前状态：已完成基础随机规则版本。奖励、商店和事件仍保留稳定回退路径，用于无候选或无随机源时保证流程可继续。

需求范围：

- 奖励池支持权重随机、稀有度权重、按当前层数过滤、按已有构筑过滤。
- 商店支持库存刷新、价格曲线、已拥有内容过滤和按构筑倾向推荐。
- 事件支持权重随机、事件去重、风险/收益权重和层数过滤。
- 保留可注入随机源，保证自动化测试可复现。

自动化测试建议：

- 覆盖权重边界、过滤规则、确定性随机种子、无可用候选时的安全回退。

完成记录：

- `RunManager` 新增可序列化随机源字段 `rng_seed` / `rng_state`，奖励、商店和事件均通过该随机源生成，自动化测试可通过固定 seed 复现结果。
- 奖励池已支持饰品按最早出现层过滤、同名饰品过滤、稀有度权重、Boss 奖励稀有倾向、已有卡组/背包/饰品标签的构筑倾向加权，以及无物品/饰品候选时回退为碎片奖励。
- 商店已支持当前节点库存缓存、手动刷新库存、刷新费用递增、随层数提升的价格曲线、已拥有饰品过滤、已购买商品刷新排除，以及基于已有构筑标签的商品推荐权重。
- 事件已支持 JSON 配置 `weight`、`risk`、`reward`、`tags` 字段，按层数过滤、已见事件去重、当前节点事件缓存、风险/收益权重调整，以及事件选项成功结算后记录 `seen_event_ids`。

实现假设：

- 构筑倾向当前采用软权重，不会因为玩家已有构筑较窄就硬性过滤掉全部其他流派候选，避免早期候选池过小导致奖励、商店或事件断档。
- 商店刷新是当前商店节点内的库存刷新，不会改变路线推进规则；玩家仍通过“离开”结束商店节点。
- 事件去重以整把游戏内已成功选择过的事件为准；若当前层所有可用事件都已见过，会回退到当前层可用事件池，避免事件节点无内容。

自动化测试：

- `test/unit/test_reward_system.gd` 覆盖固定随机种子复现、候选池为空回退碎片、Boss 奖励稀有倾向与奖励应用。
- `test/unit/test_shop_system.gd` 覆盖固定随机种子与节点缓存、刷新扣费和费用递增、价格曲线、已拥有饰品过滤与购买写入。
- `test/unit/test_event_system.gd` 覆盖事件层数过滤、已见事件过滤、节点缓存、选项记录已见事件、固定随机种子复现与事件效果回滚。

### F4. 物品获得语义细化

优先级：P2

当前状态：已完成基础语义拆分。奖励、商店、事件获得物品时可通过字段配置进入卡组、直接进入背包或进入暂存区待摆放。

需求范围：

- 明确“加入卡组”、“加入背包”、“暂存待摆放”三种语义。
- 奖励、商店和事件可以独立配置物品获得去向。
- 如果进入背包或暂存区，需要处理空间不足、取消选择、跨场景保存和 UI 展示。

自动化测试建议：

- 覆盖三种获得去向、背包满时回退、暂存区跨场景保存和取消/确认流程。

完成记录：

- `RunManager.grant_item()` 作为统一物品获得入口，支持 `deck`、`backpack`、`staging` 三种 `item_destination`。
- 关卡奖励生成的物品默认 `item_destination = deck`，保持旧流程兼容；商店生成的物品默认 `item_destination = staging`，购买后进入暂存区，玩家可在整理背包界面拖拽摆放；事件效果可在 JSON 中独立配置 `item_destination`，未配置时默认加入卡组。
- 直接进入背包时，会基于当前长期背包状态和可用区域自动寻找空位；若空间不足或缺少物品数据，会回退到暂存区，避免奖励丢失。
- 新增长期字段 `pending_item_rewards` 与 `next_pending_item_uid`，暂存物品可跨场景和存档恢复。
- 局内/整理背包 UI 新增暂存物品面板，暂存物品以可拖拽物品卡显示；成功放入背包后会消耗对应暂存项并写回长期背包状态，关闭整理背包浮层时也会持久化当前背包布局。

实现假设：

- “暂存待摆放”的取消语义为不消耗暂存项：玩家关闭整理界面、拖出背包外或未成功摆放时，暂存项仍保留在长期状态中。
- 商店购买物品优先进入暂存区，因为商店属于局外节点，玩家应在下一场局内游戏前有机会整理摆放；手写测试或旧数据中未显式声明去向的物品仍按旧逻辑加入卡组。
- 直接进入背包采用自动找位的基础版本；后续如需由玩家手动选择摆放位置，可复用同一暂存区流程。

自动化测试：

- `test/unit/test_item_acquisition_semantics.gd` 覆盖默认加入卡组、显式暂存、直接放入背包、背包满回退暂存、暂存项确认进卡组/背包、暂存存档恢复，以及商店生成物品购买后进入暂存区。

### F5. 事件系统高级效果

优先级：P2

当前状态：已完成基础高级效果版本。事件系统支持基础选择、资源增减、物品/饰品获得、梦值回复、背包可用区域扩展，以及高风险背包格变化。

需求范围：

- 支持删格、锁格、临时锁格、强制移动已有物品等高风险背包变化。
- 处理事件效果失败时的事务回滚。
- 对高风险事件提供明确 UI 预览和确认。

自动化测试建议：

- 覆盖锁格/删格后的物品搬迁、空间不足回滚、存档恢复和 UI 不可跳过。

完成记录：

- `BackpackManager` 新增 blocked cell 支持，锁定/删除/临时锁定格会被视为不可用格，放置、找空位和背包 UI 均读取同一逻辑。
- `RunManager` 新增长期字段 `backpack_locked_cells`、`backpack_deleted_cells`、`temporary_backpack_locked_cells`，并在 `get_backpack_grid_config()` 中统一输出 blocked cells，战斗和整理背包都会应用。
- 事件效果新增 `backpack_lock_cells`、`backpack_delete_cells`、`backpack_temp_lock_cells`、`backpack_force_move`。锁格或删格命中已有物品时，会先尝试把受影响物品搬迁到可用空位；搬不动时整个事件选择回滚。
- 临时锁格通过 `remaining_battles` 计数，在后续局内游戏胜利结算后递减并自动解除。
- 事件 UI 对高风险背包效果支持 `preview` 文本和二次确认：首次点击只显示确认提示，第二次点击才真正执行，避免误触。
- `data/events/events.json` 新增示例高风险事件“扭曲格纹”，用于验证锁格、临时锁格、搬迁失败保护和 UI 预览流程。

实现假设：

- “删格”和“锁格”在当前基础版中都表现为不可用格；二者保留为独立长期字段，方便后续视觉上区分“永久删除”和“锁定但仍可恢复”。
- 强制搬迁采用确定性扫描，从当前可用区域左上开始寻找第一个能完整放下物品的位置，保证测试可复现。
- 临时锁格只按已完成的局内游戏递减，不会因为商店、事件或整理背包打开而消耗持续时间。

自动化测试：

- `test/unit/test_event_system.gd` 覆盖锁格搬迁、空间不足回滚、锁格存档恢复、临时锁格随战斗结束解除，以及 blocked cells 注入 `BattleManager` 后禁止放置。

### F6. 饰品效果精修

优先级：P2

当前状态：56 个饰品已有数据框架与基础效果，复杂饰品使用通用分发实现；已按当前非道具系统完成一轮明确差异精修，并随 F1 道具系统恢复启用 51-56 号道具联动饰品。

需求范围：

- 逐个核对饰品 v1.1 策划效果与当前实现差异。
- 将高复杂度饰品从通用效果中拆为独立脚本。
- 明确每局限次、每场限次、跨战斗状态和奖励槽位修改等规则。

自动化测试建议：

- 每个高复杂度饰品至少覆盖 2-3 个核心场景，包含边界和无目标场景。

完成记录：

- 蜂蜜勺按 v1.2 结论从泛“食物”收窄为苹果或美味烧鸡，避免剩饭盒等废弃物误触发食物路线。
- 温室玻璃改为梦境之种升级基础给分，并在达到 10/20/30 级时给予额外奖励，替代旧“3级或以上”语义。
- 种子保险新增梦境之种变大因空间不足失败时的补偿，复用播种失败结算入口，保证每次捕梦后限 1 次。
- 返青符改为梦境之种达到 30 级或变为 4x4 形态时触发梦值回复，避免继续依赖旧 5 级终端语义。
- 命中计数器效果保持按本次撞击结算命中数结算。
- 终端压力表改为按本次撞击结算命中的机械物品数量结算，不再按全部命中物品数量结算。
- 齿轮油改为读取本次撞击结算的成功机械传动次数，只有机械物品成功传导或曲线传动命中时才给分，每次结算最多 5 次。
- 万向轴承改为在同一次撞击结算第一次命中机械物品时，为该物品追加一次双向传动，不再延迟为下一次独立撞击。
- 反冲片发起的反方向撞击改为机械过滤撞击，不能穿过或命中非机械物品。

实现假设：

- 道具联动新增 6 个饰品依赖 F1 道具系统，当前已恢复启用；后续精修重点是根据实机数据调整触发分数、刷新限制和与具体道具的组合边界。
- 当前梦境之种底层已改为 `dream_seed_1x1` 到 `dream_seed_4x4` 四个基础形态，并通过运行时等级支持 1-30+。温室玻璃、收获篮和返青符已改为读取真实等级与 4x4 阈值。
- 正式机械物品已提供左/右/双向/全向传动上下文；齿轮油、万向轴承、反冲片已改用该上下文或机械过滤队列。

自动化测试：

- `test/unit/test_ornament_system.gd` 覆盖蜂蜜勺正式食物判定、种子保险升级失败补偿、温室玻璃/返青符形态阈值、终端压力表只统计机械命中、齿轮油只统计成功机械传动、万向轴承同结算双向传动，以及反冲片机械过滤队列。

### F7. 表现与 UI 视觉迭代

优先级：P3

当前状态：已完成基础交互反馈统一版本。捕梦动画、UI 状态显示、tooltip、关键交互和通用按钮 hover/点击反馈已接入；正式捕梦动画、美术或 CG 需要等待素材后替换。

需求范围：

- 替换正式捕梦动画、美术或 CG。
- 统一 UI 视觉规范、图标、按钮状态、悬停反馈和音效反馈。
- 优化局内、Hub、商店、事件、奖励弹窗的一致性。

自动化测试建议：

- 保留关键场景 headless smoke test；新增 UI 状态机和输入锁的逻辑测试。

完成记录：

- `GlobalFeedback` 新增全局按钮反馈绑定，会自动为场景内和运行时动态生成的 `BaseButton` 绑定 hover 缩放、手型光标和点击音效。
- 统一反馈层覆盖主菜单、Hub 路线按钮、局内结束/奖励按钮、商店刷新/商品按钮、事件选择按钮、设置和图鉴返回按钮等现有按钮，不再需要每个场景重复写基础 hover/click 逻辑。
- 按钮反馈使用 meta 去重，并在按钮退出树时清理 tween，避免动态刷新商店、事件和奖励按钮时重复连接或残留动画引用。
- 主菜单已接入正式美术图，运行时资源为 `assets/ui/main_menu/main_menu_background.png`，原始素材来自 `assets/sourceImage/主界面.png`；场景改为全屏背景图加透明按钮热区，开始、继续、图鉴、设置和退出仍走原有导航逻辑。
- 主菜单热区按 1280x720 设计稿坐标配置，并在运行时根据 `KEEP_ASPECT_COVERED` 缩放重新布局，避免不同窗口比例下背景图与点击区域漂移。
- 无可继续存档时继续游戏热区会禁用并显示半透明遮罩，正式文字仍由美术图承载，按钮仅提供鼠标交互和 tooltip。
- 主菜单当前按美术确认回到最初暖色调整图方案；背景仍使用整图，5 个菜单书卷从该整图裁出为 `main_menu_scroll_*.png` 透明贴图并作为按钮子节点覆盖在原位置，hover 通过全局按钮反馈放大卷轴贴图而不再显示方框。
- `tools/extract_main_menu_scrolls.py` 用于从暖色主界面整图重新生成这 5 个书卷透明贴图；当前采用偏宽的手工 alpha 轮廓，优先保证撕纸边缘完整，后续若背景图重绘，需要同步更新脚本中的 1280x720 设计坐标。

实现假设：

- 当前仓库没有正式捕梦动画、美术或 CG 资源，因此本轮保留已有轻量捕梦 tween，并把正式素材替换点维持在 `MainGameUI._play_dreamcatcher_animation()`。
- 音效反馈复用已有 `ui_click.wav`；后续若补充 hover、confirm、error 等更细音效，只需要扩展 `AudioManager.SFX_PATHS` 和 `GlobalFeedback` 的按键反馈策略。
- 视觉统一先以工程行为一致为目标，不在缺少最终 UI 规范和素材时做大规模控件重绘，避免与后续正式美术导入重复返工。

自动化测试：

- `test/unit/test_ui_feedback.gd` 覆盖按钮反馈绑定幂等性、递归绑定动态按钮，以及 hover 缩放后恢复。
- `test/unit/test_main_menu_scene.gd` 覆盖主菜单正式背景图、透明按钮热区、设置容器和继续游戏禁用遮罩节点。

### F8. 经济数值调试

优先级：P3

当前状态：已完成基础数值配置化与快照测试版本。商店价格、碎片收入、刷新费用和每层购买力目标统一从 `EconomyConfig` 读取，后续调数只需要修改单一配置入口。

需求范围：

- 调整碎片产出、商品价格、奖励稀有度、Boss 奖励价值。
- 定义每层期望购买力和构筑成长速度。
- 对普通战斗无目标、Boss 有目标的收益进行平衡。

自动化测试建议：

- 建立数值快照测试，验证每层奖励和商店价格落在合理区间。

完成记录：

- 新增 `src/core/rewards/economy_config.gd` 作为经济数值的唯一配置入口，集中定义 1-6 层普通战斗碎片、Boss 战碎片、商店刷新费用、物品价格倍率和饰品稀有度价格倍率。
- `RewardGenerator` 的碎片奖励改为读取 `EconomyConfig.battle_reward_shards()`，普通战斗保持无目标局的稳定小额收益，Boss 战作为有目标局提供约 2 倍普通战斗碎片。
- `ShopGenerator` 的物品价格、饰品价格和刷新费用改为读取 `EconomyConfig`，避免奖励、商店和测试各自维护一套曲线。
- 当前基础曲线按 6 层闭环设计：每层 2 个普通战斗 + 1 个 Boss 战的战斗碎片收入为 34/42/50/58/66/74；首次刷新费用为 7/9/11/13/15/17。
- 物品价格每层 +7%，饰品基础价格每层 +9%；进阶饰品额外 +8%，稀有饰品额外 +15%，用于控制后期强力饰品的获得速度。

实现假设：

- 当前没有完整实机通关数据，因此本轮只建立可解释、可回归的基础经济曲线；真实购买率、胜率和构筑完成速度需要后续通过游玩日志继续调优。
- “每层期望购买力”暂按路线内战斗碎片收入衡量，不把玩家主动选择碎片奖励、事件收益、饰品折扣和未消费存量计入基础快照。
- 第一个商店节点仍可能买不起多数饰品，这是有意保留的早期构筑节奏；低价物品、碎片奖励和后续战斗收入会逐步打开购买空间。

自动化测试：

- `test/unit/test_economy_balance.gd` 覆盖 1-6 层经济快照、普通/Boss 碎片奖励接入、商店价格/刷新费接入，以及基础购买力区间约束。

### F9. 发布流程

优先级：P3

当前状态：已完成本地发布脚本和 GitHub Releases 自动发布基础版本。`package/` 作为本地导出和归档目录并从版本库忽略，仓库保留 `export_presets.cfg` 与可复现的 Windows 导出脚本；推送 `v*` tag 会触发 CI 导出并发布 Release。

需求范围：

- 增加本地导出脚本或 CI 导出流程。
- 将正式包发布到 GitHub Releases 或单独归档位置。
- 记录版本号、构建时间、提交号和测试结果。

自动化测试建议：

- 导出前强制运行全量 GUT 和关键场景 smoke test。

完成记录：

- 新增 `tools/export_windows_release.ps1`，默认使用 `Windows Desktop` export preset 导出 Windows 正式包。
- 导出脚本在执行 Godot export 前强制运行 `tools/run_tests_silent.ps1` 和 `scripts/run_scene_smoke_tests.py --fail-on-engine-error`，任一失败都会中止发布。
- 脚本输出到 `package/LostFragments-<构建时间>-<提交号>.exe`，同目录生成 `.manifest.json`，记录版本号、构建时间、分支、提交号、测试命令、测试结果、导出状态和产物路径。
- 支持 `-PrecheckOnly` 模式，用于在未安装导出模板或 CI 只做发布前置验证时，仍然跑完整测试并生成 manifest。
- 导出脚本支持 `-PythonBin` 参数，CI 可显式使用 `python3`，本地默认仍使用 `python` 或 `PYTHON_BIN` 环境变量。
- `tools/run_tests_silent.ps1` 的临时日志目录改为跨平台解析，Windows 使用 `TEMP`，Linux CI 可使用 `TMPDIR` 或系统临时目录，避免 Release workflow 在发布前 GUT 阶段提前失败。
- 新增 `.github/workflows/release.yml`，在推送 `v*` tag 时下载 Godot 4.6.2 与 export templates，复用本地导出脚本跑全量 GUT、严格场景冒烟和 Windows export。
- Release workflow 中 GUT 和严格场景冒烟拆成独立步骤，导出脚本通过 `-SkipPrecheck` 只负责 Windows export 与 manifest，避免 CI 失败时日志被包装脚本吞掉。
- Release workflow 使用 GitHub Actions 内置 `GITHUB_TOKEN` 和 `contents: write` 权限创建 GitHub Release，不需要提交或保存 Personal Access Token。
- CI 会把 exe 与 manifest 打包为 `LostFragments-<tag>-windows-x86_64.zip`，同时上传 manifest；包含 `alpha`、`beta` 或 `rc` 的 tag 会自动标记为 prerelease。
- README 已新增发布导出说明，包含 precheck 和正式导出命令。

实现假设：

- tag 策略采用 `v*` 触发，例如 `v0.1.0-alpha.1`、`v0.1.0-beta.1` 或 `v0.1.0`；发版时从已通过验证的 `main` 提交打 tag 并推送。
- 如果仓库设置禁用了 GitHub Actions 写权限，需要在仓库 Settings 中允许 workflow 使用 `contents: write`；正常情况下不需要额外 token。
- `export_presets.cfg` 仍是导出 preset 的唯一来源；脚本只校验 preset 存在，不在脚本中复制导出配置，避免两套发布配置漂移。

自动化测试：

- 本轮通过 `tools/export_windows_release.ps1 -PrecheckOnly` 验证发布前置流程，脚本内部已跑全量 GUT 与严格场景冒烟；Release workflow 的完整 exe 导出会在后续推送真实 `v*` tag 时由 GitHub Actions 执行。

### F10. 策划配置工具

优先级：P2

当前状态：已完成第一阶段。当前有配置 schema、校验脚本、导出脚本和经济曲线 JSON 配置，后续可在此基础上开发 Excel/CSV 导入或 Web 可视化工具。

需求范围：

- 明确哪些字段允许策划调整，哪些逻辑仍由代码实现。
- 对物品、道具、饰品、事件、路线和经济配置做基础 schema 约束。
- 提供本地校验命令，避免错误配置进入游戏。
- 提供导出命令，为后续可视化工具或策划交付包生成统一配置包。

完成记录：

- 新增 `data/config/design_config_schema.json`，记录当前可配置数据源、可编辑字段和枚举值。
- 新增 `data/economy/economy.json`，将碎片产出、商店刷新费和价格倍率迁出硬编码；`EconomyConfig` 优先读取 JSON，读取失败时回退默认值。
- 新增 `scripts/design_config/validate_design_config.py`，校验 JSON 格式、ID 唯一性、引用完整性、枚举值、路线结构、事件效果和基础数值范围。
- 新增 `scripts/design_config/export_design_config.py`，默认导出到 `package/design_config_export/`，包含 schema、道具、饰品、事件、路线、经济配置、物品目录和 manifest。
- 新增 `test/tools/test_design_config_scripts.py`，覆盖当前配置校验、物品目录提取和导出 manifest。
- 文档见 `spec/Docs/02_Tech/05_Design_Config_Tool.md`。

实现假设：

- 第一版不提供 UI 编辑器，先保证 schema、校验和导出稳定。
- 物品仍以 `.tres` 作为运行时源；导出工具先生成只读 `item_catalog.json`，后续如果需要策划直接编辑物品，再增加 Excel/CSV 到 `.tres` 或 JSON 的导入链路。
- 策划配置不能直接新增任意脚本逻辑；新效果应先由程序实现 `effect_id` 或 `type`，再开放给配置。

自动化测试：

- `python -m unittest test.tools.test_design_config_scripts`
- `python -B scripts/design_config/validate_design_config.py`
