# 当前实现路线图

文档状态：已按当前代码与 `ImplementationTODO.md` 更新。本文是总览路线图，不替代 `ImplementationTODO.md` 的逐项需求记录。

## 一、当前已完成的基础闭环

### P0 主流程骨架

- 配置化路线已迁移到 `data/routes/routes.json`。
- 默认路线为 9 个节点：局内游戏、商店、事件、局内游戏、商店、事件、Boss 局内游戏、商店、事件。
- 第 2-6 层已有独立主题路线和主题事件池，仍保持 9 节点与第 7 节点 Boss 的通关节奏。
- 游戏共 6 个场景层，完成第 6 层路线后整局胜利。
- Hub 由路线按钮驱动，只能进入当前节点，已完成和未解锁节点不可进入。
- 普通战斗默认无分数目标，Boss 战有目标分数，并在战斗结束时统一判定胜负。

### P1 局内核心规则

- `BattleManager` 已有轻量战斗状态机和统一结束请求入口。
- 梦值归零不会中断当前结算，等待本次抽取/撞击结算完成后再结束战斗。
- 背包布局可跨战斗保存，衍生物品和污染不跨战斗保存。
- 根源之梦作为默认背包物品接入，每 5 次捕梦入队发起撞击。
- 播种、梦境之种 1-30+ 等级成长、1x1-4x4 体型阈值和空间不足掉出背包已接入 `BackpackManager` 与 `GlobalEventBus`。
- 多源撞击统一进入确定性撞击队列。
- 正式 15 个机械物品、机械专用左/右/双向/全向传动和结算停止后物品效果已接入。
- 非道具饰品 v1.2 已按新版梦境之种与机械传动上下文完成一轮修正。

### P2 长期构筑、奖励与经济

- 饰品总表已对齐 56 个：全部饰品已加载并接入通用效果，6 个道具联动饰品已随 F1 道具系统启用。
- 道具系统基础版本已完成：15 个正式道具池、独立堆叠库存、局内道具栏、目标校验、成功消耗、非法目标不消耗、存档和饰品联动均已接入。
- 奖励系统支持物品、饰品、道具、碎片，以及 Boss 稀有倾向和构筑标签倾向。
- 商店支持物品、饰品、道具、节点缓存、库存刷新、价格曲线、已购买排除和回收券折扣。
- 事件系统支持权重随机、去重、节点缓存、事务回滚和高风险背包效果。
- 物品获得语义已拆分为加入卡组、直接入背包、暂存待摆放。
- 经济曲线已迁移到 `data/economy/economy.json`，`EconomyConfig` 保留默认回退，并有数值快照测试。
- 策划配置工具第一阶段已完成：schema、校验脚本、导出脚本和物品目录导出均已接入。

### P3 表现、测试与发布

- 全局 tooltip、污染动态显示、暂存物品面板和奖励选择 UI 已接入。
- 全局按钮 hover/点击反馈已接入 `GlobalFeedback`。
- 关键场景 headless smoke runner 已接入本地和 CI。
- 本地发布脚本 `tools/export_windows_release.ps1` 已支持 precheck、正式导出和 manifest 记录。

## 二、当前有效需求队列

当前有效队列以 `ImplementationTODO.md` 为准：

1. P0 清理旧术语、旧数据和旧物品池：已完成。正式物品池已移除隔离箱，污染不再自动扣梦值，用户可见文案统一到梦值与撞击结算。
2. P0 重构撞击结算上下文：已完成。每个撞击源会创建独立上下文，按物品去重并记录命中统计；机械传动分支和饰品 v1.2 已复用该上下文。
3. P1 梦境之种重构：已完成。已从旧版 1-5 级/5x5 形态对齐为 1-30+ 等级与 1x1-4x4 体型。
4. P1 机械传动正式实现：已完成。已实现左/右传动、双向传动、全向传动和正式 15 个机械物品效果。
5. P1 非道具饰品 v1.2 修正：已完成。命中计数器、终端压力表、齿轮油、万向轴承、反冲片、种子保险、温室玻璃、返青符等已对齐新版口径。
6. P2 道具联动饰品恢复：已完成。51-56 号饰品已启用并接入道具使用、丢弃和撞击上下文。
7. F1 道具系统基础版本：已完成。正式 15 个道具池、独立库存、局内道具栏、目标校验、消耗、存档、奖励/商店/事件获得入口和饰品联动已接入。
8. 用户视频反馈的 UI 问题：已完成基础修复。整理背包浮层有独立模式、鼠标关闭按钮、UI 输入上下文保护和居中背包布局；Hub 支持鼠标点击移动。

## 三、下一轮建议开发顺序

### 1. 道具数值和获取规则调优

F1 基础闭环已经完成，下一步不再是补架构，而是补策划数值：

- 明确道具在普通奖励、Boss 奖励、商店和事件中的正式出现概率。
- 调整 `data/tools/tools.json` 中价格和效果数值。
- 结合实机日志观察道具栏使用频率、无目标失败率和强组合收益。
- 根据正式数值精修 51-56 号道具联动饰品。

### 2. 视觉资产替换

正式捕梦动画、CG、美术和统一 UI 规范需要素材输入。当前代码保留替换点：

- `MainGameUI._play_dreamcatcher_animation()`
- `GlobalFeedback` 的按钮反馈策略
- `main_game_ui.tscn` 和 `backpack_ui.tscn` 的布局资源

### 3. 发布自动化

GitHub Releases 已接入基础自动发布：推送 `v*` tag 后，CI 会下载 Godot 4.6.2 与 export templates，复用 `tools/export_windows_release.ps1` 跑全量 GUT、严格场景冒烟和 Windows export，再创建 Release 并上传 zip 与 manifest。后续只需要根据试玩节奏决定具体 tag 版本号。

## 四、验证矩阵

| 改动类型 | 必跑验证 |
| --- | --- |
| 纯文档 | `git diff --check` |
| 逻辑功能 | `.\tools\run_tests_silent.ps1` |
| UI、场景、资源、autoload | `.\tools\run_tests_silent.ps1` + `python -B scripts\run_scene_smoke_tests.py --fail-on-engine-error` |
| 策划配置、数据表、经济数值 | `python -B scripts\design_config\validate_design_config.py` + `.\tools\run_tests_silent.ps1` |
| 发布流程 | `.\tools\export_windows_release.ps1 -PrecheckOnly` |
| 导出前 | 发布 precheck 必须通过 |

## 五、历史路线说明

早期文档曾将“污染流机制”作为当前阶段目标。该目标已经被主流程、路线、奖励、商店、事件、饰品、经济和发布流程的基础闭环取代；后续不要再按旧“污染流当前目标”推进。
